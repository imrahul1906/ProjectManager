package src.task;

/**
 * Task attributes class
 */
class TaskAttributes {
    /**
     * All the attributes of a Task.
     */
    public enum Attributes {
        TARGET_BRANCH,
        TARGET_VERSION,
        DETECTED_VERSION,
        TITLE,
        SUMMARY,
        DESCRIPTION,
        ATTACHMENTS,
        ASSIGNEE,
        REPORTER,
        COMPONENTS
    }
}
